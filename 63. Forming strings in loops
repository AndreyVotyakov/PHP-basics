<?php

function mysubstr($str, $len)
{
    $i = 0;
    $result = '';

    while ($i < $len) {
        $currentChar = $str[$i];
        $result = "{$result}{$currentChar}";
        $i = $i + 1;
    }

    return $result;
}
